path = File.dirname(__FILE__)

workers 2
app_id "simple example"
queue "rpc"
adapter :bunny

rack_file File.join(path, "config.ru")

before_fork do |server|
  server.logger.info "BEFORE FORK"
end

after_fork do |server, worker|
  server.logger.info "AFTER FORK"
end

